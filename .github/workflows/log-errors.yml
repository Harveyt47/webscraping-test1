name: Generate issue from error and warning in our logs
on: push

jobs:
  errorReport:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.error_report.outputs.matrix }}
    steps:
      - name: checkout repo content
        uses: actions/checkout@v2 # checkout the repository content to github runner.
      - name: setup python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8 #install the python needed#OUTPUT=$(python ApiError.py '282053' )
      - name: Run error report
        id: error_report
        run: |
              pip install scrapinghub
              echo "::set-output name=matrix::hello"
      - name: print output
        run: |
             echo "${{ steps.error_report.outputs.maxtrix }}"
            
      
